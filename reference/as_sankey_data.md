# Convert one of numerous data types to sankey_network's 'native' data format

The `sankey_network` function uses a 'native' data format that consists
of a...

## Usage

``` r
as_sankey_data(.data, ...)
```

## Arguments

- .data:

  a sankey network description in one of numerous forms (see details).

- ...:

  other arguments that will be passed on to as_sankey_data
